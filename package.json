{
  "name": "litetube",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "test": "node --test",
    "deploy:worker": "cd cloudflare && npx wrangler deploy -c wrangler.toml",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.54.4",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.2.0",
    "wrangler": "^4.129.0"
  }
}
